#! /bin/bash
# This file is intended for github workflows that runs on ubuntu machines. You can use this script only on linux distros that has apt as its package manager
set -e

sudo apt-get update
sudo apt install libgtk-4-1 libgtk-4-dev gtk-4-examples

# Install just
sudo apt install just
