#include "renderer.h"
#include "app_data.h"
#include "glib.h"
#include "gtk/gtk.h"

void render(GtkApplication* app, gpointer user_data) {
    AppData* data = user_data;

    // gtk_widget_queue_draw();
}
