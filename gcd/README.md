# GCD

This directory is for compatibility. GCD being a tool developped by me and not
available on package managers, it also comes with a config script to map sockery
directly to avoid the TUI to launch
